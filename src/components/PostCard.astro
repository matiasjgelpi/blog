---
export interface Props {
  title: string;
  description: string;
  href: string;
}
const { title, description, href } = Astro.props;
---
<article style="border:1px solid #eee; border-radius:8px; padding:1rem; margin:1rem 0; box-shadow:0 2px 8px #eee;">
  <h2>{title}</h2>
  <p>{description}</p>
  <a href={href} style="color:#0070f3; text-decoration:underline;">Leer más</a>
</article>
